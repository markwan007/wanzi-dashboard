[build]
  publish = "."
  command = "node build.js"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# Environment variables will be injected at build time
[build.processing]
  skip_processing = false
